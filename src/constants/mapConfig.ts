/**
 * Mapbox configuration
 * APP_SETUP: Replace with your Mapbox public access token.
 * Get one at https://account.mapbox.com/access-tokens/
 *
 * Secret download token (for native builds) goes in:
 *   - app.json > plugins > @rnmapbox/maps > RNMapboxMapsDownloadToken
 *   - EAS secret: MAPBOX_SECRET_TOKEN
 */
export const MAPBOX_ACCESS_TOKEN = 'YOUR_MAPBOX_PUBLIC_TOKEN';
