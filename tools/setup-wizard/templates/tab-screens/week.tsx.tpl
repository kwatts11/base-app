/**
 * Week tab — generated by Base-App Setup Wizard.
 */
import React from 'react';
import { View, StyleSheet } from 'react-native';

import { useTheme } from '../../src/context/ThemeProvider';
import { WeekView } from '../../src/components/time-index/WeekView';
import { PageIdDisplay } from '../../src/components/common';
import { PAGE_IDS } from '../../src/constants/version';

export default function WeekScreen(): React.JSX.Element {
  const { theme } = useTheme();
  return (
    <View style={[styles.screen, { backgroundColor: theme.colors.background }]}>
      <PageIdDisplay pageId={PAGE_IDS.WEEK as any} />
      <WeekView tableName="{{TABLE_NAME}}" />
    </View>
  );
}

const styles = StyleSheet.create({ screen: { flex: 1 } });
