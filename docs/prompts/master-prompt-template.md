# Setup: {{APP_NAME}} — Final AI steps (autonomous)

> **Generated by Base-App Setup Wizard on {{GENERATED_DATE}}**
>
> The wizard already configured: app identity, theme, roles, enums, tabs, scaffolds,
> migrations (applied via Supabase pooler), email branding, and PRD.md.
> Three short steps remain. Execute all of them in order without asking questions.

---

## App context

- App name: **{{APP_NAME}}**
- Supabase project: `{{SUPABASE_PROJECT_ID}}`

For full configuration details see `PRD.md` and `SETUP_TODO.md` in the project root.

---

## 1. Generate TypeScript types from Supabase

Call the Supabase MCP `generate_typescript_types` tool with `project_id: "{{SUPABASE_PROJECT_ID}}"`.

Merge the result into `src/types/database.ts`:

- **Keep** the hand-written `UserRole` enum and any helper types/utility types
- **Replace/extend** the per-table `Row` interfaces and the `Database` interface
- Re-run the merge until `npx tsc --noEmit` is clean

If the wizard reported that migration apply was skipped (no DB password, or pooler
connection failed), first run `apply_migration` MCP for each file under
`database/migrations/` in numeric order before generating types.

## 2. Install dependencies and start

```bash
npm install
npm start
```

Fix any TypeScript or runtime errors that appear. Common issues:

- Missing import in a generated tab screen → add the import
- `Database` table missing → re-run step 1
- Mapbox plugin error → check `app.json` and the value of `MAPBOX_PUBLIC_TOKEN` in `.env`

## 3. Confirm SETUP_TODO.md is complete

Open `SETUP_TODO.md` and either complete the remaining manual items or report which
ones still require user action (SMTP verification, first admin invite, deployment).

When all three steps are done, summarize what's left for the user to do manually.
